# どういう構造にしたいか

- まずは js を使わない想定で rust 用の api を持つ構造体を定義する
    - どのように host 側から api を保持、呼び出しするのか?
        - エントリーポイントをどうするのか問題
        - trait { fn new(api: Api) } みたいなトレイトを実装する形にするとか?
    - どのように js へラップするのか？
        - 特に何か永続的なデータを持たせたい場合は JS 側の変数とのメモリの扱いをどうするのかというのが難しそう

- FunctionBuilder から外の変数を操作できるようにする
    - closure で変数をキャプチャしたりするのはできないみたい
    - なので、何か別の方法でこれを解決する必要がある
    - FunctionBuilder.data()
- js のコールバック関数を呼び出せるようにする
